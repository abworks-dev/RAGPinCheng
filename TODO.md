# 品成 BIM 知识库 — 待办事项 Roadmap

按优先级排序的功能开发计划。

---

## 🔴 高优先级 — 已有明确设计方案

### 1. 查询拆分 / 多跳检索 — Phase 2

**适用场景**：用户提问需要对比/合并多个实体或主题，但**没有规范编号**做为确定性触发信号。

示例问题：
- "对比 Revit 族与 Dynamo 节点的复用方式"
- "客户A和客户B对图层命名的不同要求"
- "钢结构和混凝土结构在BIM建模流程上的差异"

**设计方案**：
- [ ] 新增 Prompt `prompts/decompose_system.md`
- [ ] 使用便宜模型（`LLM_REWRITE_MODEL = glm-4.5-air`）判断是否需要拆分
- [ ] 返回 JSON 格式：`{"decompose": bool, "sub_queries": [...]}`，最多 3 个子查询
- [ ] 启发式触发 gate：query 包含 `对比|比较|区别|差异|分别|VS|vs` 等比较标记时才调用 LLM，预计触发率 <5%/turn
- [ ] 拆分后复用已有的 `retrieve_multi` 多路检索 + 全局重排（Phase 1 基础设施已就绪）

**涉及文件**：
- 新增 `prompts/decompose_system.md`
- 修改 `src/session.py` 或新增 `src/decompose.py`
- 补充黄金集对比型测试用例

---

## 🟡 中优先级 — 已有基础实现，需完善

### 2. 跨父文档多轮上下文携带

**现状**：当前 `ChatSession` 只携带上一轮最优 2 个来源的片段，局限在同一文档内的连续追问。

**待实现**：
- [ ] 记录每轮所有命中父文档的元数据（doc_title、category、section_path）
- [ ] 后续轮次自动将相关历史文档的标题作为软提示注入重写器
- [ ] 极端情况下可展开历史来源的完整文本（需额外预算控制）
- [ ] 支持真正的"跨文档综合"复杂多轮对话

**参考**：`scripts/append_handwritten.py:27`

---

## 🟢 体验增强 — 无破坏性，逐步迭代

### 3. 视频转录本播放器集成

**现状**：
- `doc_type: "transcript"` + `start_time` 已在 `SourceDTO` 中完整传递
- 前端目前只显示 `🎬 @HH:MM:SS` 文本标记

**待实现**：
- [ ] 后端 `parents.sqlite` 新增视频文件可访问 URL 字段
- [ ] 前端实现内嵌播放器或跳转播放功能
- [ ] 点击回答中的引用角标时，跳转到视频对应时间点
- [ ] 来源面板中添加"播放"按钮

> ⚠️ 注意：根据 `frontend.md` 规则，此功能在实现前只能描述为"补充报告中的候选实现"，不得写成现有能力。

### 4. 来源面板增强功能

**文件**：`frontend/src/components/SourcesPanel.tsx`

待实现：
- [ ] "复制来源"按钮（复制文档标题 + 章节路径）
- [ ] 来源按分类分组显示
- [ ] 高亮来源文本中与问题匹配的关键词
- [ ] 来源卡片直接跳转到对应 PDF 页码（需 PDF.js 集成）
- [ ] 支持批量导出引用列表

---

## 📋 运维与质量

### 5. 反馈日志分析仪表盘

**现状**：`api/feedback.py` 已实现数据收集接口，用户可报告引用错误。

**待实现**：
- [ ] 管理员后台可视化反馈趋势图表
- [ ] 按文档/章节聚合报错率
- [ ] 检索失败案例的自动聚类分析
- [ ] 定时生成检索质量报告

### 6. 检索黄金集扩展

**文件**：`src/eval/`

**当前基线**（2026-05）：R@1 = 90%, R@5 = 96%, no-answer = 100%（~97 条）

**待补充**：
- [ ] 更多对比型问题（Phase 2 上线前需要）
- [ ] 多轮对话案例
- [ ] 边界 case（纯数字、纯代码、无意义输入）
- [ ] 视频转录本相关问题

---

## ✅ 最近已完成（已从待办移除）

按时间倒序：

1. ✅ **SSE 心跳机制防止流超时断开** — `api/routes_chat.py`（2026-07-22）
2. ✅ **纯数字输入无论第几轮对话都拦截** — `src/query_guard.py`（2026-07-22）
3. ✅ **引用角标 tooltip 闪烁、遮挡、溢出修复** — `Message.tsx`（2026-07-22）
4. ✅ **WPS 风格引用角标样式** — `Message.tsx`（2026-07-22）
5. ✅ **移除 LLM 输出中重复的"资料来源"小节** — `prompts/answer_system.md`（2026-07-22）
6. ✅ **查询验证守卫（无意义输入拦截）** — `src/query_guard.py`（2026-07-20）
7. ✅ **Docker 构建缓存优化与依赖清理** — `requirements-prod.txt`、`docker/*`（2026-07-20）
8. ✅ **Code-boost 规范编号检索增强** — `src/retrieve.py`（2026-05）
9. ✅ **表格摘要索引增强** — `src/table_summary.py`（2026-05）
10. ✅ **每轮遥测 + 每用户聊天限流** — `api/rate_limit.py`（2026-05）

---

## 📝 备注

- 本文件替代了原有的纯文本 TODO 文件
- 所有 RAG 相关修改必须遵循 `.claude/rules/rag-pipeline.md` 中的黄金集验证规则
- 涉及 Schema 变更必须说明索引兼容性和是否需要 `--reset`
- 新增 Prompt 统一放在 `prompts/*.md` 并通过 `src/prompts.py` 加载
