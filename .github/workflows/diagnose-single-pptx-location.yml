name: Diagnose All Production PPTX Locations

on:
  workflow_dispatch:
    inputs:
      confirm_diagnostic:
        description: Select DIAGNOSE_ALL_PPTX to parse all current production PPTX files read-only
        required: true
        default: CANCEL
        type: choice
        options:
          - CANCEL
          - DIAGNOSE_ALL_PPTX

permissions:
  contents: read

concurrency:
  group: production-app-manual-v1
  cancel-in-progress: false

jobs:
  diagnose:
    environment: production-asr
    runs-on: [self-hosted, linux, ubuntu, production, app]
    timeout-minutes: 30
    env:
      REPO_PATH: ${{ vars.PRODUCTION_APP_REPO_PATH }}
      DATA_PATH: ${{ vars.PRODUCTION_APP_DATA_PATH }}
      CONTENT_ROOT: ${{ vars.PRODUCTION_CONTENT_ROOT }}
      CONFIRM_DIAGNOSTIC: ${{ inputs.confirm_diagnostic }}
    steps:
      - name: Check out the approved diagnostic implementation
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Parse the exact PPTX in a disposable read-only container
        shell: bash
        run: |
          set -euo pipefail

          [ "${CONFIRM_DIAGNOSTIC}" = "DIAGNOSE_ALL_PPTX" ] || {
            echo "ERROR: production all-PPTX diagnostic was not confirmed"
            exit 1
          }
          [ "${GITHUB_REF}" = "refs/heads/master" ] || {
            echo "ERROR: production single-PPTX diagnostic must run from master"
            exit 1
          }
          [ "${REPO_PATH}" = "/data/workspace/projects/ragpincheng" ] || {
            echo "ERROR: production application repository is not the approved path"
            exit 1
          }
          [ "${DATA_PATH}" = "/data/services/docker/data/ragpincheng/prod/app" ] || {
            echo "ERROR: production application data directory is not the approved path"
            exit 1
          }
          [ "${CONTENT_ROOT}" = "/data/business/ragpincheng/content" ] || {
            echo "ERROR: production content root is not the approved path"
            exit 1
          }
          for path in "${REPO_PATH}" "${DATA_PATH}" "${CONTENT_ROOT}"; do
            [ -d "${path}" ] && [ ! -L "${path}" ] || {
              echo "ERROR: an approved production directory is unavailable"
              exit 1
            }
          done
          APP_DATABASE="${DATA_PATH}/app.sqlite"
          [ -f "${APP_DATABASE}" ] && [ ! -L "${APP_DATABASE}" ] || {
            echo "ERROR: production application database is unavailable"
            exit 1
          }
          [ "$(git -C "${REPO_PATH}" branch --show-current)" = "master" ] || {
            echo "ERROR: production application repository must be on master"
            exit 1
          }
          [ -z "$(git -C "${REPO_PATH}" status --porcelain --untracked-files=no)" ] || {
            echo "ERROR: production application repository has tracked local changes"
            exit 1
          }
          docker image inspect pincheng-rag-backend:latest >/dev/null

          TEMP_ROOT="$(mktemp -d)"
          trap 'rm -rf -- "${TEMP_ROOT}"' EXIT
          export APP_DATABASE CONTENT_ROOT TEMP_ROOT
          python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          import shutil
          import sqlite3

          with sqlite3.connect(f"file:{os.environ['APP_DATABASE']}?mode=ro", uri=True) as connection:
              rows = connection.execute(
                  """SELECT h.item_id,h.current_version_id,o.storage_rel_path,o.sha256,v.original_filename,v.doc_type
                       FROM content_item_heads h
                       JOIN content_versions v
                         ON v.id=h.current_version_id AND v.item_id=h.item_id
                       JOIN content_objects o ON o.sha256=v.object_sha256
                      WHERE v.doc_type='pptx' ORDER BY h.item_id"""
              ).fetchall()
          root = Path(os.environ["CONTENT_ROOT"]).resolve(strict=True)
          manifest = []
          for index, row in enumerate(rows):
              source = (root / str(row[2])).resolve(strict=True)
              if source == root or root not in source.parents or not source.is_file() or source.is_symlink():
                  raise SystemExit("ERROR: approved production PPTX object is unsafe")
              expected = str(row[3])
              if hashlib.sha256(source.read_bytes()).hexdigest() != expected:
                  raise SystemExit("ERROR: approved production PPTX object hash mismatch")
              target = Path(os.environ["TEMP_ROOT"]) / f"{index:04d}.pptx"
              shutil.copyfile(source, target)
              if hashlib.sha256(target.read_bytes()).hexdigest() != expected:
                  raise SystemExit("ERROR: temporary PPTX copy hash mismatch")
              manifest.append({"item_id": row[0], "version_id": row[1], "original_filename": row[4], "sha256": expected, "source": f"/diagnostic-input/{target.name}"})
          (Path(os.environ["TEMP_ROOT"]) / "manifest.json").write_text(json.dumps(manifest), encoding="utf-8")
          PY

          OUTPUT="$(docker run --rm \
            --read-only \
            --network none \
            --pids-limit 512 \
            --tmpfs /tmp:rw,noexec,nosuid,size=2g \
            --tmpfs /diagnostic-work:rw,exec,nosuid,size=4g \
            -e HOME=/tmp \
            -e PYTHONDONTWRITEBYTECODE=1 \
            -e PYTHONPATH=/diagnostic/repo \
            -e RAG_DATA_DIR=/diagnostic-work/data \
            -e RAG_PARSED_DIR=/diagnostic-work/parsed \
            -e CONTENT_ROOT=/diagnostic-work/content \
            -e DOCS_DIR=/diagnostic-work/docs \
            -e MEDIA_DIR=/diagnostic-work/media \
            -v "${TEMP_ROOT}:/diagnostic-input:ro" \
            -v "${GITHUB_WORKSPACE}:/diagnostic/repo:ro" \
            --entrypoint python3 \
            pincheng-rag-backend:latest \
            /diagnostic/repo/scripts/diagnose_single_pptx_location.py \
              --manifest /diagnostic-input/manifest.json \
              --work-dir /diagnostic-work)"
          printf '%s\n' "${OUTPUT}"
          DIAGNOSTIC_JSON="$(printf '%s\n' "${OUTPUT}" | sed -n \
            's/^PPTX_ALL_LOCATION_DIAGNOSTIC //p')"
          [ -n "${DIAGNOSTIC_JSON}" ] || {
            echo "ERROR: structural diagnostic output is missing"
            exit 1
          }
          DIAGNOSTIC_JSON="${DIAGNOSTIC_JSON}" python3 - <<'PY'
          import json
          import os

          report = json.loads(os.environ["DIAGNOSTIC_JSON"])
          if report.get("schema_version") != 1:
              raise SystemExit("ERROR: unsupported structural diagnostic report")
          print("PPTX_ALL_LOCATION_DIAGNOSTIC_VALIDATED status=read_only")
          PY
          {
            echo "## All production PPTX location diagnostic"
            echo
            echo "- Status: read-only parse completed"
            echo
            echo '```json'
            printf '%s\n' "${DIAGNOSTIC_JSON}"
            echo '```'
          } >> "${GITHUB_STEP_SUMMARY}"
