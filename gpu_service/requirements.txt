# GPU Inference Service — runtime dependencies.
# Install on the Windows GPU host:
#   pip install -r gpu_service\requirements.txt
#
# NOTE: torch >= 2.7 is required for RTX 5060 Ti (Blackwell sm_120).
# The default PyPI index serves the CPU-only build.  Install the CUDA build
# FIRST via the instructions below, then let this file satisfy the rest.
#
#   pip install --index-url https://download.pytorch.org/whl/cu128 "torch>=2.7"
#   pip install -r gpu_service\requirements.txt
#
# To verify CUDA is available:
#   python -c "import torch; print(torch.cuda.is_available())"

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Inference — same versions as the production Docker container
FlagEmbedding>=1.3.0,<2
transformers>=4.46,<5

# Utilities
python-dotenv>=1.0.0