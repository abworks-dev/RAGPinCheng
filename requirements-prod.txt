# Production container deps — see requirements.txt for full local-dev set.
# Excludes:
#   - mineru[core]  (cloud API used via MINERU_API_KEY; local CLI fallback not shipped)
#   - streamlit     (React frontend is the production UI)
# torch is installed separately in the Dockerfile from the cu128 index
# (CUDA 12.8, required for the RTX 5060 Ti / Blackwell sm_120). Pinning it
# there means this `pip install` sees torch already satisfied and won't pull
# a different build. See docker/Dockerfile.backend for the wheel + the host
# GPU prerequisites.
requests>=2.32.0
pypdf>=4.0.0
langchain-text-splitters>=0.3.0
FlagEmbedding>=1.3.0
# Pin transformers to 4.x. FlagEmbedding 1.4 still calls the legacy
# `tokenizer.prepare_for_model(...)` API which was removed in transformers 5.x,
# producing `AttributeError: XLMRobertaTokenizer has no attribute
# prepare_for_model` at rerank time. Lower bound is set high enough to keep
# the torch>=2.6 CVE-2025-32434 check happy.
transformers>=4.46,<5
qdrant-client>=1.12.0
openai>=1.50.0
python-dotenv>=1.0.0
tqdm>=4.66.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
# Auth: argon2 password hashing for users table.
passlib[argon2]>=1.7.4
# Required by FastAPI for multipart/form-data parsing — admin upload endpoint.
python-multipart>=0.0.20
