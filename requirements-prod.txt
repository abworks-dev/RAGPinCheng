# Production container deps — see requirements.txt for full local dev set.
# Excludes:
#   - mineru[core]  (cloud API used via MINERU_API_KEY; local CLI fallback not shipped)
#   - streamlit     (React frontend is the production UI)
# torch is installed separately in the Dockerfile from the cu128 index
# (CUDA 12.8, required for RTX 5060 Ti / Blackwell sm_120). Pinning it
# there means this pip install sees torch as already satisfied and won't
# pull a different build.
requests>=2.32.0
pypdf>=4.0.0
langchain-text-splitters>=0.3.0
# Qdrant client — still needed for the API <-> vector DB link.
qdrant-client>=1.12.0
# Office document parsing — DOCX/PPTX to Markdown via Docling Slim.
docling>=2.0.0
openai>=1.50.0
python-dotenv>=1.0.0
tqdm>=4.66.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
passlib[argon2]>=1.7.4
python-multipart>=0.0.20
