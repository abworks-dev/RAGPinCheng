# GPU inference development dependencies.
# Production promotion must use gpu_service/runtime-lock.json and
# gpu_service/runtime-lock.txt; this file must never be installed globally.
FlagEmbedding>=1.3.0,<2
transformers>=4.44.2,<6
tokenizers>=0.20,<1
