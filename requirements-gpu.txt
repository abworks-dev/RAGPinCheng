# GPU inference development dependencies.
# Production promotion must use services/gpu_service/runtime-lock.json and
# services/gpu_service/runtime-lock.txt; this file must never be installed globally.
FlagEmbedding>=1.3.0,<2
# Keep these bounds identical to services/gpu_service/requirements.txt: transformers
# >= 4.56.0 provides the `dtype` keyword FlagEmbedding 1.4.0 requires, and
# tokenizers must stay >=0.22,<0.23 or transformers is capped back at 4.55.4.
transformers>=4.56,<5
tokenizers>=0.22,<0.23
